import React from 'react';
import {render} from '@testing-library/react';

import UserInfo from '../index';

describe('# UserInfo', () => {
  // TODO
  test('Must render UserInfo component and compare changes', () => {
    // const tree = render(<UserInfo />);
    // expect(tree).toMatchSnapshot();
  });
});
